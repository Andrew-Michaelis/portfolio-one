import '../App.css';
import Button from './Button';

export default function NavigationBar() {
  return (
    <div className="Navigation-bar">
      <Button btn="header"/>
      <Button />
      <Button />
    </div>
  )
}